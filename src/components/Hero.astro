---
import { Image } from "astro:assets";
import heroFaces from "../assets/hero-faces.png";
---

<section
    class="text-center pt-12 pb-24 px-6 max-w-6xl mx-auto relative overflow-hidden"
>
    <!-- Faces Banner -->
    <div class="flex justify-center mb-6 animate-fade-in-up">
        <Image
            src={heroFaces}
            alt="Replicate Notion Team Faces"
            class="h-24 md:h-32 w-auto object-contain"
            loading="eager"
            widths={[96, 128, 256]}
            sizes="(min-width: 768px) 128px, 96px"
        />
    </div>

    <h1
        class="animate-fade-in-up text-5xl md:text-7xl font-bold leading-tight tracking-tight text-gray-900 mb-6"
    >
        One workspace.<br />Zero busywork.
    </h1>

    <p
        class="animate-fade-in-up mt-6 text-lg md:text-xl text-gray-600 max-w-2xl mx-auto mb-8"
        style="animation-delay: 0.1s; animation-fill-mode: backwards;"
    >
        Replicate Notion is where your teams and AI agents capture knowledge,
        find answers, and automate projects. Now a team of 7 feels like 70.
    </p>

    <div
        class="flex flex-col sm:flex-row justify-center gap-4 mb-20 animate-fade-in-up"
        style="animation-delay: 0.2s; animation-fill-mode: backwards;"
    >
        <button
            class="bg-notion-blue text-white px-8 py-3 rounded-lg font-bold text-base hover:bg-blue-600 transition-all hover:shadow-lg"
        >
            Get Replicate Notion free
        </button>
        <button
            class="bg-white text-notion-blue px-8 py-3 rounded-lg font-bold text-base hover:bg-blue-50 transition-colors"
        >
            Request a demo
        </button>
    </div>

    <!-- Hero Image Placeholder -->
    <div
        class="relative max-w-5xl mx-auto mb-20 animate-on-scroll"
        style="transition-delay: 300ms"
    >
        <div
            class="bg-white rounded-xl shadow-2xl border border-gray-200 p-2 overflow-hidden"
        >
            <!-- Replicating the interface preview -->
            <div
                class="bg-white rounded-lg h-64 md:h-[500px] flex flex-col text-left p-4 md:p-8"
            >
                <div
                    class="w-full h-8 flex items-center gap-2 border-b border-gray-100 pb-2 mb-4"
                >
                    <div class="w-3 h-3 rounded-full bg-red-400"></div>
                    <div class="w-3 h-3 rounded-full bg-yellow-400"></div>
                    <div class="w-3 h-3 rounded-full bg-green-400"></div>
                </div>
                <div class="space-y-4">
                    <div class="h-8 w-1/3 bg-gray-100 rounded animate-pulse">
                    </div>
                    <div class="h-4 w-3/4 bg-gray-50 rounded animate-pulse">
                    </div>
                    <div class="h-4 w-1/2 bg-gray-50 rounded animate-pulse">
                    </div>
                    <div class="h-4 w-full bg-gray-50 rounded animate-pulse">
                    </div>
                </div>
                <!-- Add a visual "card" inside -->
                <div
                    class="mt-8 p-6 bg-yellow-50 rounded-xl border border-yellow-100 max-w-md mx-auto shadow-sm"
                >
                    <div class="font-bold text-yellow-900 mb-2 text-lg">
                        🚀 Project Launch
                    </div>
                    <p class="text-yellow-800 text-sm">
                        Reviewing Q1 goals and assigning tasks to the team.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <!-- Trusted Teams -->
    <div class="text-center">
        <p class="text-gray-500 font-medium mb-8">Trusted by top teams</p>
        <div
            class="flex flex-wrap justify-center gap-8 md:gap-16 opacity-60 grayscale hover:grayscale-0 transition-all duration-500"
        >
            <!-- Simple SVG Placeholders for Brands -->
            <div class="font-bold text-2xl flex items-center gap-2">
                <div class="w-6 h-6 bg-black rounded-full"></div> OpenAI
            </div>
            <div class="font-bold text-2xl flex items-center gap-2">
                <div class="w-6 h-6 border-2 border-black rounded relative">
                    <div class="absolute top-0 right-0 w-2 h-2 bg-black"></div>
                </div> Figma
            </div>
            <div class="font-bold text-2xl flex items-center gap-2">ramp</div>
            <div class="font-bold text-2xl flex items-center gap-2">CURSOR</div>
            <div class="font-bold text-2xl flex items-center gap-2">
                ▲ Vercel
            </div>
            <div class="font-bold text-2xl flex items-center gap-2">NVIDIA</div>
        </div>
    </div>
</section>
