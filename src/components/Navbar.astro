<nav
    class="sticky top-0 z-50 bg-[#050511]/80 backdrop-blur-md border-b border-white/5 transition-colors"
>
    <!-- Main Navbar Container -->
    <div class="flex items-center justify-between px-6 py-4">
        <!-- Logo Section -->
        <div class="flex items-center gap-2">
            <img
                src="/images/logo-n-geometric.png"
                alt="Logo"
                class="w-8 h-8 rounded-sm"
            />
            <h1 class="text-xl font-bold font-inter tracking-tight text-white">
                Nexa
            </h1>
        </div>

        <!-- Desktop Menu Links -->
        <div class="hidden md:flex gap-6 text-sm font-medium text-gray-300">
            <a
                href="/"
                class="hover:bg-gray-100 px-3 py-2 rounded-md transition-colors"
                >Product</a
            >
            <a
                href="/download"
                class="hover:bg-white/10 px-3 py-2 rounded-md transition-colors"
                >Download</a
            >
            <a
                href="/pricing"
                class="hover:bg-white/10 px-3 py-2 rounded-md transition-colors"
                >Pricing</a
            >
            <a
                href="/resources"
                class="hover:bg-white/10 px-3 py-2 rounded-md transition-colors"
                >Resources</a
            >
        </div>

        <!-- Right Side Actions -->
        <div class="flex items-center gap-4">
            <a
                href="#"
                class="hidden md:block text-sm font-medium hover:bg-white/10 px-3 py-2 rounded-md transition-colors"
                >Log in</a
            >
            <button
                class="bg-notion-blue text-white px-4 py-2 rounded-lg text-sm font-bold hover:bg-blue-600 transition-colors"
            >
                Get Nexa Free
            </button>

            <!-- Hamburger Button -->
            <button
                id="menu-toggle"
                class="md:hidden p-2 -mr-2 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-200 transition-colors"
                aria-label="Toggle menu"
                aria-expanded="false"
            >
                <!-- Icon changes based on state (handled via JS or CSS classes could optionally swap icons, but rotation is simpler) -->
                <svg
                    class="w-6 h-6 transition-transform duration-200"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                    id="menu-icon"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
            </button>
        </div>
    </div>

    <!-- Mobile Navigation Menu (Overlay) -->
    <div
        id="mobile-menu"
        class="hidden absolute top-full left-0 w-full bg-[#050511] border-b border-white/10 shadow-xl flex-col p-4 space-y-4 md:hidden origin-top animate-fade-in-down"
    >
        <div class="flex flex-col space-y-1">
            <a
                href="#"
                class="block px-4 py-3 text-base font-medium text-gray-300 hover:bg-white/5 hover:text-white rounded-lg transition-colors"
                >Product</a
            >
            <a
                href="#"
                class="block px-4 py-3 text-base font-medium text-gray-300 hover:bg-white/5 hover:text-white rounded-lg transition-colors"
                >Download</a
            >
            <a
                href="#"
                class="block px-4 py-3 text-base font-medium text-gray-300 hover:bg-white/5 hover:text-white rounded-lg transition-colors"
                >Pricing</a
            >
            <a
                href="#"
                class="block px-4 py-3 text-base font-medium text-gray-300 hover:bg-white/5 hover:text-white rounded-lg transition-colors"
                >Resources</a
            >
        </div>

        <div class="border-t border-gray-100 pt-4 flex flex-col gap-3">
            <a
                href="#"
                class="block px-4 py-2 text-base font-medium text-center text-gray-300 hover:bg-white/5 rounded-lg transition-colors"
                >Log in</a
            >
            <button
                class="w-full bg-notion-blue text-white px-4 py-3 rounded-lg text-sm font-bold hover:bg-blue-600 transition-colors"
            >
                Get Nexa Free
            </button>
        </div>
    </div>
</nav>

<script>
    const menuToggle = document.getElementById("menu-toggle");
    const mobileMenu = document.getElementById("mobile-menu");
    const menuIcon = document.getElementById("menu-icon");

    if (menuToggle && mobileMenu) {
        menuToggle.addEventListener("click", () => {
            const isHidden = mobileMenu.classList.contains("hidden");

            if (isHidden) {
                // Open Menu
                mobileMenu.classList.remove("hidden");
                mobileMenu.classList.add("flex");
                menuToggle.setAttribute("aria-expanded", "true");
                // Simple icon animation
                if (menuIcon)
                    menuIcon.innerHTML =
                        '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>';
            } else {
                // Close Menu
                mobileMenu.classList.add("hidden");
                mobileMenu.classList.remove("flex");
                menuToggle.setAttribute("aria-expanded", "false");
                if (menuIcon)
                    menuIcon.innerHTML =
                        '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>';
            }
        });
    }
</script>
